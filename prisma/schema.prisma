datasource db {
  provider = "sqlite"
}

generator kysely {
  provider = "prisma-kysely"

  output   = "../src/api/db/generated"
  fileName = "types.ts"
}

model users {
  id       Int    @id @default(autoincrement())
  name     String
  password String
}
